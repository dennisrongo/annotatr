<project_specification>
  <project_name>annotatr</project_name>

  <overview>
    A cross-platform screen annotation overlay tool that lets users draw arrows, circles, boxes, freehand drawings, highlights, and text on their screen in real-time while recording with any screen capture software (OBS, Loom, Zoom, etc.). Users trigger shapes with configurable global hotkeys or a mini panel, draw on screen, and shapes auto-fade after a configured duration. Perfect for creating engaging tutorials, presentations, and screen recordings with visual annotations.
  </overview>

  <technology_stack>
    <frontend>
      <framework>React with TypeScript</framework>
      <styling>CSS with styled-components or Tailwind CSS</styling>
      <additional_config>Component-based UI architecture with TypeScript strict mode</additional_config>
    </frontend>
    <backend>
      <runtime>Tauri 2 (Rust backend)</runtime>
      <database>Local storage (Tauri's persistent storage API)</database>
      <additional_config>Platform-specific window management for overlays</additional_config>
    </backend>
    <communication>
      <api>Tauri's event system for IPC between frontend and Rust backend</api>
      <additional_config>Global hotkey registration via Tauri plugins</additional_config>
    </communication>
  </technology_stack>

  <prerequisites>
    <environment_setup>
      - Node.js and npm/yarn installed
      - Rust toolchain installed
      - Tauri 2 CLI installed
      - Platform-specific build tools (Xcode for macOS, Visual Studio for Windows)
    </environment_setup>
  </prerequisites>

  <feature_count>135</feature_count>

  <security_and_access_control>
    <user_roles>
      <role name="user">
        <permissions>
          - Can create and configure annotation shapes
          - Can modify all application settings
          - Can access global hotkey functionality
          - Cannot access system-level resources beyond screen overlay
        </permissions>
        <protected_routes>
          - None (single-user desktop application)
        </protected_routes>
      </role>
    </user_roles>
    <authentication>
      <method>none - local desktop application</method>
      <session_timeout>none</session_timeout>
      <password_requirements>n/a</password_requirements>
    </authentication>
    <sensitive_operations>
      - Global hotkey registration requires system permissions
      - Screen overlay requires platform-specific permissions
    </sensitive_operations>
  </security_and_access_control>

  <core_features>
    <Infrastructure>
      - Local storage connection established
      - Settings persistence verified across app restarts
      - No mock data patterns in codebase
      - Tauri API integration properly configured
      - Cross-platform build system functional
    </Infrastructure>

    <Core_Overlay_System>
      - Transparent overlay window created on top of other applications
      - Overlay captures mouse input when in drawing mode
      - Overlay positioned per-monitor based on cursor location
      - Multi-monitor support (shapes confined to single monitor)
      - Overlay can be dismissed via Escape key or hotkey toggle
      - Platform-appropriate overlay implementation (Windows/macOS/Linux)
      - Consistent visual styling across all platforms
      - Overlay window management (show/hide/focus)
      - Click-through prevention during drawing mode
      - Z-index management to stay above other windows
      - Cursor changes when entering drawing mode
      - Visual indicator showing active drawing tool
      - Overlay activation via hotkey or mini panel
      - Mini panel can be positioned off-screen to hide from recordings
      - Overlay deactivation and shape cleanup
    </Core_Overlay_System>

    <Drawing_Tools>
      - Arrow tool (click and drag to create arrow)
      - Circle tool (click and drag to create circle/ellipse)
      - Box tool (click and drag to create rectangle/box)
      - Freehand drawing tool (draw and release)
      - Highlighter tool (semi-transparent freehand drawing)
      - Text tool (click spot and type)
      - Tool selection via mini panel buttons
      - Tool selection via global hotkeys
      - Tool switching while in drawing mode
      - Each tool has configurable color
      - Each tool has configurable line thickness
      - Current tool visual indicator (cursor change)
      - Shape preview while drawing (real-time rendering)
      - Shape completion on mouse release
      - Multiple shapes can exist on screen simultaneously
      - Newer shapes appear on top of older shapes
      - Drawing mode activation
      - Drawing mode deactivation
      - Cancel drawing mode without creating shape (Escape/hotkey)
      - Tool-specific default settings
    </Drawing_Tools>

    <Mini_Panel_UI>
      - Mini panel window with tool selection buttons
      - Arrow tool button in panel
      - Circle tool button in panel
      - Box tool button in panel
      - Freehand tool button in panel
      - Highlighter tool button in panel
      - Text tool button in panel
      - Visual indicator for currently selected tool
      - Color picker with preset colors
      - Color picker with custom color selection
      - Line thickness slider/control
      - Font size control for text
      - Settings button to open settings panel
      - Panel can be moved by user
      - Panel can be positioned on any monitor
      - Panel stays on top of other windows
      - Panel minimization/hide option
      - Responsive button hover states
      - Accessible button labels/tooltips
      - Panel styling consistent with app theme
    </Mini_Panel_UI>

    <Hotkey_System>
      - Global hotkey for Arrow tool (Ctrl+Shift+A)
      - Global hotkey for Circle tool (Ctrl+Shift+C)
      - Global hotkey for Box tool (Ctrl+Shift+B)
      - Global hotkey for Freehand tool (Ctrl+Shift+F)
      - Global hotkey for Text tool (Ctrl+Shift+T)
      - Global hotkey for Highlighter tool (Ctrl+Shift+H)
      - Global hotkey to toggle drawing mode (Ctrl+Shift+D)
      - Hotkey conflict detection with system apps
      - Hotkey customization in settings
      - Hotkey registration/deregistration
      - Hotkey persistence across app restarts
      - Visual feedback when hotkey triggers
      - Escape key to cancel drawing mode
      - Same hotkey toggles drawing mode on/off
    </Hotkey_System>

    <Text_Input>
      - Click-to-type text placement
      - On-screen text input field at click location
      - Text rendering on overlay
      - Font size configuration
      - Text color follows current color selection
      - Text submission on Enter or click away
      - Text editing before placement
      - Text positioning follows cursor
      - Text shape participates in auto-fade
    </Text_Input>

    <Auto-Fade_System>
      - Configurable fade duration (default 10 seconds)
      - Per-shape fade timer started on creation
      - Fade animation for each shape individually
      - Smooth opacity transition (fade out effect)
      - Shape removal after fade completes
      - Fade duration configurable in settings
      - Fade persists across tool switches
      - Fade animation triggered near end of lifetime
      - Visual cue during fade (pulsing or gradual opacity)
    </Auto-Fade_System>

    <Settings_and_Persistence>
      - Settings panel accessible from mini panel
      - Hotkey configuration UI
      - Color picker (presets + custom)
      - Line thickness slider with value display
      - Font size slider with value display
      - Auto-fade duration input (seconds)
      - Save settings button
      - Reset to defaults button
      - Settings saved to local storage
      - Settings loaded on app startup
      - Default settings on first launch (red color, 12px thickness, 12-14pt font, 10s fade)
      - Settings validation (no negative values, reasonable ranges)
      - Settings persistence verification
      - Real-time settings preview
    </Settings_and_Persistence>

    <Cross_Platform>
      - Windows overlay implementation
      - macOS overlay implementation
      - Linux overlay implementation
      - Consistent UI styling across platforms
      - Platform-specific permissions handling
      - Platform-specific build configurations
      - Global hotkey support on Windows
      - Global hotkey support on macOS
      - Global hotkey support on Linux
    </Cross_Platform>
  </core_features>

  <database_schema>
    <tables>
      <settings>
        - id (primary key)
        - key (string, unique)
        - value (JSON/serialized)
        - created_at (timestamp)
        - updated_at (timestamp)
      </settings>
    </tables>
  </database_schema>

  <api_endpoints_summary>
    <Tauri_Commands>
      - invoke:save_settings (settings object)
      - invoke:load_settings
      - invoke:register_hotkeys (hotkey config)
      - invoke:create_overlay_window
      - invoke:show_overlay
      - invoke:hide_overlay
      - invoke:set_overlay_position (monitor, x, y)
      - invoke:drawing_start (tool, x, y)
      - invoke:drawing_update (x, y)
      - invoke:drawing_end (shape_data)
      - invoke:create_shape (shape_data)
      - invoke:clear_all_shapes
      - invoke:start_fade_timer (shape_id, duration)
      - invoke:cancel_fade_timer (shape_id)
    </Tauri_Commands>
    <Events>
      - event:hotkey_triggered (tool_name)
      - event:shape_created (shape_data)
      - event:shape_fade_complete (shape_id)
      - event:drawing_mode_changed (is_active)
      - event:settings_updated (settings)
    </Events>
  </api_endpoints_summary>

  <ui_layout>
    <main_structure>
      Mini panel window (draggable, stay-on-top):
      - Horizontal toolbar with tool buttons
      - Color picker dropdown
      - Line thickness and font size controls
      - Settings button

      Overlay window (transparent, full-screen per monitor):
      - Canvas for rendering shapes
      - Captures mouse when drawing mode active
      - Invisible when not in drawing mode

      Settings modal:
      - Form-based layout with sections
      - Hotkey configuration table
      - Sliders for numeric values
      - Color picker
      - Save/Reset buttons
    </main_structure>
  </ui_layout>

  <design_system>
    <color_palette>
      - Primary: Modern blue/purple accent
      - Default drawing color: Red (#FF0000)
      - Panel background: Semi-transparent dark or light neutral
      - Button hover: Slightly lighter/darker shade
      - Text: High contrast for readability
    </color_palette>
    <typography>
      - System font stack (San Francisco on Mac, Segoe UI on Windows, etc.)
      - Text tool default: 12-14pt
      - UI labels: 11-12pt
    </typography>
  </design_system>

  <implementation_steps>
    <step number="1">
      <title>Infrastructure & Project Setup</title>
      <tasks>
        - Initialize Tauri 2 project with React/TypeScript
        - Configure build system for cross-platform support
        - Set up local storage API integration
        - Verify persistence across app restarts
        - Create basic window management system
      </tasks>
    </step>
    <step number="2">
      <title>Core Overlay System</title>
      <tasks>
        - Implement transparent overlay window
        - Add multi-monitor detection and positioning
        - Implement mouse capture in drawing mode
        - Add platform-specific overlay implementations
        - Create overlay show/hide functionality
      </tasks>
    </step>
    <step number="3">
      <title>Mini Panel UI</title>
      <tasks>
        - Build React components for panel UI
        - Create tool selection buttons
        - Add color picker (presets + custom)
        - Implement line thickness and font size controls
        - Make panel draggable and positionable
      </tasks>
    </step>
    <step number="4">
      <title>Drawing Tools Implementation</title>
      <tasks>
        - Implement arrow drawing (click and drag)
        - Implement circle/ellipse drawing
        - Implement box/rectangle drawing
        - Implement freehand drawing
        - Implement highlighter tool
        - Add shape rendering on overlay canvas
      </tasks>
    </step>
    <step number="5">
      <title>Text Tool</title>
      <tasks>
        - Implement click-to-type interaction
        - Add on-screen text input field
        - Render text on overlay
        - Add font size configuration
      </tasks>
    </step>
    <step number="6">
      <title>Hotkey System</title>
      <tasks>
        - Register global hotkeys via Tauri
        - Implement hotkey-to-tool mapping
        - Add hotkey conflict detection
        - Create hotkey configuration UI
        - Add Escape key cancellation
      </tasks>
    </step>
    <step number="7">
      <title>Auto-Fade System</title>
      <tasks>
        - Implement per-shape fade timers
        - Add fade animation (opacity transition)
        - Create configurable fade duration setting
        - Handle shape cleanup after fade
      </tasks>
    </step>
    <step number="8">
      <title>Settings & Persistence</title>
      <tasks>
        - Create settings modal UI
        - Implement settings save/load
        - Add default values on first launch
        - Implement settings validation
        - Verify persistence across restarts
      </tasks>
    </step>
    <step number="9">
      <title>Cross-Platform Polish</title>
      <tasks>
        - Test and refine Windows implementation
        - Test and refine macOS implementation
        - Test and refine Linux implementation
        - Ensure consistent styling
        - Handle platform-specific edge cases
      </tasks>
    </step>
  </implementation_steps>

  <success_criteria>
    <functionality>
      - All 6 drawing tools (arrow, circle, box, freehand, highlighter, text) work correctly
      - Global hotkeys trigger tools and toggle drawing mode
      - Mini panel allows tool selection and configuration
      - Shapes appear on top of other applications for recording
      - Shapes auto-fade after configurable duration with smooth animation
      - Settings persist across app restarts
      - Multi-monitor support works correctly
      - Drawing mode captures mouse and prevents clicking through
      - Tool switching works via hotkeys and panel
      - Escape key cancels drawing mode
    </functionality>
    <user_experience>
      - Intuitive mini panel with clear tool indicators
      - Responsive drawing with real-time shape preview
      - Smooth fade animations
      - Easy settings configuration
      - Minimal startup time
    </user_experience>
    <technical_quality>
      - Clean TypeScript code with proper typing
      - Proper error handling for platform-specific APIs
      - Efficient rendering (no lag during drawing)
      - Memory efficient (shapes cleaned up after fade)
      - No mock data or hardcoded values
    </technical_quality>
    <design_polish>
      - Consistent modern UI across platforms
      - Smooth animations and transitions
      - Accessible button labels and tooltips
      - Professional color scheme
      - Polished settings panel
    </design_polish>
  </success_criteria>
</project_specification>
